## Customize the test machine
machine:

  timezone:
    Asia/Taipei

  python:
    version:
      2.7.11

  services:
    - docker

dependencies:
  pre:
    - cat $(find -name requirements.txt) | pip install -r /dev/stdin

## Customize test commands
test:
  override:
    - make lint
    - make coverage && mv htmlcov/* $CIRCLE_ARTIFACTS/

notify:
  webhooks:
    - url: https://compose.phacility.com/harbormaster/hook/circleci/
